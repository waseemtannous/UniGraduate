# UniGraduate

## Run Locally

client:

```bash
cd client
npm install
npm start
```

server:

```bash
cd server
npm install
npm sun server
```

## Run Using Docker

```bash
docker-compose up -d --build
```
